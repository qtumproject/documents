{
  "name": "documents",
  "scripts": {
    "start": "gitbook serve",
    "build": "gitbook build . docs",
    "publish": "git add docs && git commit -m 'update gitbook build' && git push origin master",
    "deploy": "npm run build && npm run publish"
  },
  "version": "1.0.0",
  "description": "qtum documentation project",
  "main": "index.js",
  "repository": "git@github.com:qtumproject/documents.git",
  "author": "Howard Yeh <howard@metacircus.com>",
  "license": "MIT",
  "dependencies": {
    "gitbook-cli": "^2.3.2"
  }
}
